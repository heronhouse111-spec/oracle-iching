# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# Claude Code local settings (per-machine, not shared)
.claude/

# typescript
*.tsbuildinfo
next-env.d.ts

# secrets — 絕對不能進 repo
api key appbackend.txt
*.key
*.secret
# 手動操作筆記容易夾帶 token / keystore 密碼,一律不 commit
supabase/手動變成訂閱者.txt
supabase/變回免費會員.txt
supabase/*訂閱者*.txt
supabase/*管理員*.txt

# brand source assets(8MB+ 原檔,不進 git,改存本機/雲端硬碟)
logo-source.png
